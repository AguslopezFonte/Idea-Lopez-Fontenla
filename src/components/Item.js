
import './Item.css';

function Card({product, price, img}) {
  return (
      <div className='card'>
          <h4>{props.product}</h4>
          <p>{props.price}</p>
          <img src={img} />
      </div>


  );
  <button className='Boton1'>Ver detalle del producto</button>
}

export default Card; 